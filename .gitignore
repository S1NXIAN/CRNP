# Environment configuration (contains secrets — never commit)
.env

# Uploads (user-generated content)
uploads/*
!uploads/.htaccess
!uploads/.gitkeep

# OS files
.DS_Store
Thumbs.db

# Runtime logs
*.log

# Composer (dev tooling — never deployed)
vendor/
composer.lock

# Tooling caches
.php-cs-fixer.cache
.phpunit.result.cache
.phpunit.cache/

# Editor / IDE
.idea/
*.swp
